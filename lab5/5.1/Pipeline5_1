pipeline {
  agent {
    kubernetes {
      //cloud 'kubernetes'
      //defaultContainer ''
      yaml """
kind: Pod
spec:
  containers:
  - name: gradle-build
    image: iptcp/gradle-app:0.3
    imagePullPolicy: IfNotPresent
    command: ["./gradlew"]
    args: ["build; sleep 300"]
"""
    }
  }
  stages {
    stage('Building app') {
      steps {
        echo "Running gradle build"
      }
    }
    stage('Running app') {
      steps {
        container('iptcp/gradle-app:0.3') {
        sh 'java -version'
        echo "Running gradle app"
        sh './gradlew run'
        }
      }
    }
    stage('Smoke test') {
      steps {
        container('iptcp/gradle-app:0.3') {
        echo 'Smoke test'
        sh './gradlew run | grep "Hello World!"'
        }
        }
        }
    stage('Upload artifact to GCP storage') {
      steps {
        container('iptcp/gradle-app:0.3') {
        googleStorageUpload bucket: 'gs://simple-pipeline-artifact2', credentialsId: 'simple-pipeline-app', pattern: 'app/build/distributions/*' 
        }
        }
    } 
  }
}

