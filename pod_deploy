pipeline {
  agent {
    kubernetes {
      label 'app'
      containerTemplate {
        name 'app-template'
        image 'iptcp/simple-app:0.5'
        ttyEnabled true
        command 'cat'
      }
    }
  }
  stages {
    stage('Deploy simple-app') {
      steps {
        echo 'Deploying simple app'
      }
    }
  }
}